{
  "name": "molina-arjay-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost --open=/",
    "start": "live-server --port=3000 --host=localhost --open=/"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0"
  }
}
